# 리스트(List)
리스트는 여러 값을 하나의 변수에 순서대로 저장하는 자료 구조

> 리스트 생성
```python
scores = [80, 90, 100, 70]
print(scores)           # 모든 요소 출력
print(scores[0])        # 첫 번째(0번) 원소
print(scores[-1])       # 뒤에서 첫 번째 원소
print(len(scores))      # 길이(원소 개수) - `len`
```

---

> 출력결과
```text
80
70
4
```

---
